## Running tests

Use `python3 -m pytest` from the project root.

Plain `pytest` now resolves the project modules too, but `python3 -m pytest` is the documented command to keep execution consistent with the active interpreter.
